import NavbarHeader from './navbar-header';
import Navbar from './navbar';

export default function Header() {
    return (
        <header>

            <NavbarHeader />

            <Navbar />

        </header>
    )
};